import { USER_ROLES } from './userRoles.js'

/** B2C mobile app — individual + labour only */
export const APP_B2C_ROLES = [USER_ROLES.INDIVIDUAL, USER_ROLES.LABOUR]

export const CORPORATE_ROLES = [USER_ROLES.CORPORATE]

export const VENDOR_ROLES = [USER_ROLES.CONTRACTOR]
